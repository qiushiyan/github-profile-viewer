import axios from "axios"
import { useState } from "react"

const rootURL = 'https://api.github.com';


const useSearch = async (user) => {
    // request loading
    axios(`{rootURL}`)
}

export default useSearch